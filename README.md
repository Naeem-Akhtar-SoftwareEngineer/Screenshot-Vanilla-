# Screenshot-Vanilla-
A lightweight screenshot capture tool built with pure HTML, CSS, and JavaScript (Vanilla JS). This project allows users to capture screenshots of specific sections or the entire webpage directly from the browser — without using any external libraries or frameworks. It demonstrates practical knowledge of DOM manipulation, and browser APIs.
